import { render } from 'react-dom';
import { App } from './App';

// Primeiro Parametro - O que vai ser renderizado na tela
// Pegundo Parametro - qual elemento no html vai ser manipulado
render(<App />, document.getElementById('root'))